# /test

Run all 100 F1 race simulation test cases and report results.

```bash
./test_runner.sh
```

After running, summarize:
- Total score (X/100)
- Which test numbers failed (if any)
- Pattern in failures (e.g., high track_temp races, multi-stop strategies)
- Suggested next debugging step based on the score tier in `docs/faq.md`
